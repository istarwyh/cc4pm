# 网站本地验收记录

日期：2026-09-06。本文记录发布前的本地验收；线上部署结果以 GitHub Actions 中本次提交对应的 Pages 运行结果为准。

## 内容与构建

- 产品课程 78 节（26 主线、52 补充），律师课程 10 节，共 88 节；完整生成 153 个 HTML 页面。
- 发布 11 个普通互动 HTML 页面、共享 CSS、共享实操须知，以及 28 份律师案例、模板和参考交付件。微信专用变体不发布。
- 固定 Hugo Extended 0.165.0 与 OINK v1.0.0；严格构建无警告。分别以 `https://istarwyh.github.io/cc4pm/` 和 `https://example.org/` 构建通过。
- 生产子路径构建检查 17,651 处本地链接、锚点和资源引用；确认 Markdown、`llms.txt`、导航 JSON、sitemap、404 与每课唯一 H1。统计随后续内容变化而变化。
- 构建前后的课件哈希一致。临时副本验证课件标题与正文修改可同步到阅读页、目录和首页，删除旧生成页不残留；全部课件代码块内容保持不变。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| `npm test` | 原项目 736 项测试通过 |
| `npm run test:site` | 5 项网站测试通过，含复制提示成功/拒绝路径 |
| `npm run check:homepage` | 独立首页与共享生成器一致 |
| 新增 Markdown、站点模块和互动页内联脚本语法 | 通过 |
| `actionlint` 1.7.11 | 两个变更工作流通过，未启用额外 ShellCheck |
| `bash -n scripts/release.sh`、`git diff --check` | 通过 |
| 首页包本地打包与读取 | HTML 与源产物一致，`filePath` / `html()` API 可用 |

首页包版本已准备为 1.0.14，未发布到 npm。主项目版本保持 2.0.6。根 npm 包 dry-run 检查网站源码、生成目录和 Hugo 缓存不进入安装包。

## 浏览器检查

在 Chrome 中检查桌面宽度 1272px 和手机宽度 390px：

- 首页正文及课程数量默认可见，移动菜单可展开，页面无横向溢出。
- 产品目录默认 78 节，勾选“只看主线”后显示 26 节、隐藏 52 节补充课。
- “上下文”“CLAUDE.md”“MCP”“17.10”均检索到对应课件；通过搜索结果进入 Lesson 5。
- 阅读页侧栏、页内目录及明暗主题可用，手机导航可以展开。
- Lesson 5 → 互动目录页 → 点击 CLAUDE.md 卡片 → 返回课程可用。
- 浏览器拒绝剪贴板访问时，显示可选中的学习提示；成功复制路径另由自动测试覆盖。
- 律师材料页可进入模板分类，原文件 HTTP 下载返回 200，字节与源模板一致；不存在的地址返回 404。

## 待线上发布后验证

Pages 实际部署、线上响应与缓存需在发布时验证。首页包正式发布改为手动运行工作流，Pages 推送只校验首页包，不发布 npm 或通知下游。现有外部图片、字体与视频仍依赖第三方可用性；全站静态链接检查不代表外部链接都已验证。
